# Firebase (Client) — same project as the web app
# Get these from: Firebase Console → Project Settings → General → Web app config
# Or copy from your web app's .env.local (replace NEXT_PUBLIC_ with EXPO_PUBLIC_)
EXPO_PUBLIC_FIREBASE_API_KEY=
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=
EXPO_PUBLIC_FIREBASE_PROJECT_ID=
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
EXPO_PUBLIC_FIREBASE_APP_ID=

# Backend API base URL (your deployed Next.js web app)
# Local dev: http://<your-local-ip>:3000 (NOT localhost — your phone can't reach localhost)
# Production: https://your-app.vercel.app
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:3000
